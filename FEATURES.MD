# FEATURES

- **SEO Audit capabilities:** Fully functional with Google API dependencies pre-built via Composer.
- **Dependency Management:** Managed via `composer.json` and locked with `composer.lock`.
